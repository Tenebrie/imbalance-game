export default class GameTimeMessage {
    constructor(currentTime, maximumTime) {
        this.currentTime = currentTime;
        this.maximumTime = maximumTime;
    }
}
//# sourceMappingURL=GameTimeMessage.js.map